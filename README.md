# gn-hj.
This project implements a basic backend REST API for managing user records. Data is stored in memory (no external database required), and the API follows standard REST conventions with proper HTTP status codes and input validation.
